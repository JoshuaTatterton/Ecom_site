<% if can?(:view, Pim::Product) %>
  <ul class="navbar-nav">
    <li class="nav-item dropdown">
      <a class="nav-link dropdown-toggle" id="product_management_nav" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <%= render "generic/bootstrap_icon", icon: "tags" %>
        Product Management
      </a>

      <ul class="dropdown-menu">
        <% if can? :view, Pim::Product %>
          <li>
            <%= link_to "Products", admin_products_path(Switch.current_account), class: "dropdown-item" %>
          </li>
        <% end %>
      </ul>
    </li>
  </ul>
<% end %>
